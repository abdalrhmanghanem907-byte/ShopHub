// ============================================================
// CENTRAL SEO CONFIGURATION
// ------------------------------------------------------------
// This is the single source of truth for SEO URLs and branding.
//
// IMPORTANT: Before deploying to production, replace `SITE_URL`
// below with your real domain. Do NOT scatter URLs elsewhere.
// ============================================================

// TODO: Replace with your real production domain before deployment.
export const SITE_URL = "https://example.com";

// Brand / site metadata
export const SITE_NAME = "ShopAbdalrhman";

// Default image used for social sharing (Open Graph / Twitter cards).
// There is currently no dedicated social share image, so we leave this
// empty (og:image / twitter:image will be omitted) rather than inventing
// a fake URL. Product pages pass their own real product image instead.
// Replace with a dedicated social share image before going live.
export const DEFAULT_IMAGE = "";

// Default meta description (home page).
export const DEFAULT_DESCRIPTION =
  "ShopAbdalrhman is an online store offering electronics, fashion, accessories, and home & living products.";
